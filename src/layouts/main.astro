---
import "@/styles/global.css"
import { execSync } from "node:child_process"
import { Font } from "astro:assets"
import { faq } from "@/data/faq"

function getLastModified() {
  try {
    const committedAt = execSync("git log -1 --format=%cI", {
      encoding: "utf8",
    }).trim()
    if (committedAt) return new Date(committedAt).toISOString()
  } catch {}
  return new Date().toISOString()
}

const lastModified = getLastModified()

interface Props {
  title?: string
  description?: string
  noindex?: boolean
}

const {
  title = "Sprawdź pomysł na aplikację w 4 tygodnie | Karol Modelski",
  description = "Masz pomysł na aplikację? W 4 tygodnie sprawdzisz, czy ludzie chcą z niej korzystać – bez agencji IT i przepalania budżetu.",
  noindex = false,
} = Astro.props

const canonicalURL = new URL(Astro.url.pathname, Astro.site)
const ogImage = new URL("/og-image.png", Astro.site)

const structuredData = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebPage",
      "@id": "https://karolmodelski.pl/#webpage",
      url: "https://karolmodelski.pl/",
      name: title,
      description,
      inLanguage: "pl-PL",
      isPartOf: { "@id": "https://karolmodelski.pl/#website" },
      about: { "@id": "https://karolmodelski.pl/#organization" },
      dateModified: lastModified,
    },
    {
      "@type": "Organization",
      "@id": "https://karolmodelski.pl/#organization",
      name: "Karol Modelski",
      url: "https://karolmodelski.pl/",
      logo: "https://karolmodelski.pl/logo.png",
      image: "https://karolmodelski.pl/og-image.png",
      email: "kontakt@karolmodelski.pl",
      telephone: "+48664598563",
      taxID: "6112800950",
      description:
        "Twój techniczny partner w tworzeniu aplikacji i produktów cyfrowych. Prowadzę projekt i zespół specjalistów, uruchamiając pierwszą działającą wersję w 4 tygodnie.",
      areaServed: "PL",
      knowsLanguage: "pl",
      sameAs: [
        "https://www.linkedin.com/in/karol-modelski/",
        "https://g.page/r/CZSVfAGtTiIzEBM",
      ],
      contactPoint: {
        "@type": "ContactPoint",
        contactType: "sales",
        email: "kontakt@karolmodelski.pl",
        telephone: "+48664598563",
        availableLanguage: ["pl"],
      },
      founder: { "@id": "https://karolmodelski.pl/#person" },
    },
    {
      "@type": "Person",
      "@id": "https://karolmodelski.pl/#person",
      name: "Karol Modelski",
      url: "https://karolmodelski.pl/",
      image: "https://karolmodelski.pl/karol-modelski.webp",
      jobTitle: "Twórca aplikacji i produktów cyfrowych",
      description:
        "Techniczny partner pomysłodawców i inwestorów: prowadzi projekt i koordynuje zespół specjalistów, zamieniając pomysły w działające aplikacje webowe w 4 tygodnie, z gwarancją terminu i pełnym prawem własności po stronie klienta.",
      knowsAbout: [
        "tworzenie aplikacji",
        "Pierwsza działająca wersja aplikacji w 4 tygodnie",
        "produkty cyfrowe",
        "walidacja pomysłów na rynku",
        "React",
        "Astro",
      ],
      sameAs: [
        "https://www.linkedin.com/in/karol-modelski/",
        "https://g.page/r/CZSVfAGtTiIzEBM",
      ],
      worksFor: { "@id": "https://karolmodelski.pl/#organization" },
    },
    {
      "@type": "WebSite",
      "@id": "https://karolmodelski.pl/#website",
      url: "https://karolmodelski.pl/",
      name: "Karol Modelski",
      inLanguage: "pl-PL",
      publisher: { "@id": "https://karolmodelski.pl/#organization" },
    },
    {
      "@type": "Service",
      "@id": "https://karolmodelski.pl/#proces",
      name: "Proces w 4 tygodnie – od pomysłu do działającej aplikacji webowej",
      provider: { "@id": "https://karolmodelski.pl/#organization" },
      serviceType: "Tworzenie aplikacji i produktów cyfrowych",
      areaServed: "PL",
      url: "https://karolmodelski.pl/#konsultacja",
      description:
        "Tworzenie działającej aplikacji webowej, własnej strony i podglądu zachowań użytkowników w 4 tygodnie – z gwarancją terminu i pełnym prawem własności po stronie klienta.",
      offers: {
        "@type": "Offer",
        url: "https://karolmodelski.pl/#konsultacja",
        description:
          "Pierwsza działająca wersja w 4 tygodnie, pełny odbiór z poprawkami zwykle 6–8 tygodni. Przy opóźnieniu z mojej winy pracuję bezpłatnie aż do skutku, a kary umowne klient potrąca z kolejnej faktury; 3 miesiące gwarancji technicznej po wdrożeniu.",
        priceCurrency: "PLN",
        price: "29500",
      },
    },
    {
      "@type": "FAQPage",
      "@id": "https://karolmodelski.pl/#faq",
      mainEntity: faq.map(({ question, answer }) => ({
        "@type": "Question",
        name: question,
        acceptedAnswer: {
          "@type": "Answer",
          text: answer,
        },
      })),
    },
  ],
}
---

<html lang="pl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="canonical" href={canonicalURL} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta name="theme-color" content="#ffffff" />
    <Font cssVariable="--font-fraunces" preload />
    <Font cssVariable="--font-plex-sans" preload />
    <Font cssVariable="--font-plex-mono" />

    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content="Karol Modelski" />
    {noindex && <meta name="robots" content="noindex, follow" />}

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Karol Modelski" />
    <meta property="og:locale" content="pl_PL" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta
      property="og:image:alt"
      content="Karol Modelski – pierwsza działająca wersja Twojego pomysłu w 4 tygodnie"
    />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    <meta
      name="twitter:image:alt"
      content="Karol Modelski – pierwsza działająca wersja Twojego pomysłu w 4 tygodnie"
    />

    <script
      type="application/ld+json"
      set:html={JSON.stringify(structuredData)}
    />

    <script is:inline>
      ;(function () {
        var root = document.documentElement
        try {
          var saved = localStorage.getItem("theme")
          if (
            saved === "dark" ||
            (!saved && matchMedia("(prefers-color-scheme: dark)").matches)
          )
            root.classList.add("dark")
        } catch (e) {}

        document.addEventListener("click", function (ev) {
          var t = ev.target
          if (t && t.closest && t.closest("#theme-toggle")) {
            var dark = root.classList.toggle("dark")
            try {
              localStorage.setItem("theme", dark ? "dark" : "light")
            } catch (e) {}
          }
        })

        function ready(fn) {
          if (document.readyState !== "loading") fn()
          else document.addEventListener("DOMContentLoaded", fn)
        }

        ready(function () {
          var reduce = matchMedia("(prefers-reduced-motion: reduce)").matches

          var sections = document.querySelectorAll("main > section[id]")
          if (!reduce && "IntersectionObserver" in window && sections.length) {
            sections.forEach(function (s) {
              s.classList.add("will-reveal")
            })
            var io = new IntersectionObserver(
              function (entries) {
                entries.forEach(function (e) {
                  if (e.isIntersecting) {
                    e.target.classList.add("is-visible")
                    io.unobserve(e.target)
                  }
                })
              },
              { rootMargin: "0px 0px -8% 0px", threshold: 0.06 }
            )
            sections.forEach(function (s) {
              io.observe(s)
            })
          }

          var counters = document.querySelectorAll("[data-count]")
          if (reduce || !counters.length || !("IntersectionObserver" in window))
            return
          var fmt = function (n) {
            return String(Math.round(n)).replace(
              /\B(?=(\d{3})+(?!\d))/g,
              "\u00a0"
            )
          }
          var cio = new IntersectionObserver(
            function (entries) {
              entries.forEach(function (e) {
                if (!e.isIntersecting) return
                var el = e.target
                cio.unobserve(el)
                var target = parseFloat(el.getAttribute("data-count"))
                var suffix = el.getAttribute("data-suffix") || ""
                var start = performance.now()
                var dur = 1100
                function tick(now) {
                  var p = Math.min((now - start) / dur, 1)
                  var eased = 1 - Math.pow(1 - p, 3)
                  el.textContent = fmt(target * eased) + suffix
                  if (p < 1) requestAnimationFrame(tick)
                  else el.textContent = fmt(target) + suffix
                }
                requestAnimationFrame(tick)
              })
            },
            { threshold: 0.4 }
          )
          counters.forEach(function (c) {
            cio.observe(c)
          })
        })
      })()
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
